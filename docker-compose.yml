services:
  dm-retriever:
    build: .
    container_name: dm-retriever
    restart: unless-stopped
    ports:
      - "8946:8946"          # Change the left number to use a different host port
    volumes:
      - ./downloads:/downloads   # Videos land in ./downloads on your machine
    environment:
      - DOWNLOAD_DIR=/downloads